# Copie este arquivo para `.env` (na mesma pasta) e preencha com credenciais reais.
# O arquivo `.env` é gitignored — nunca commitar.

PORTAL_BASE_URL=https://localhost:58495
PORTAL_SEGMENTO=filantropia
PORTAL_USER=<usuario>
PORTAL_PASS=<senha>
